<template>
	<div>INDEX</div>
</template>

<script>
import Vue from 'vue';

export default Vue.extend({
	name: 'Index',
	asyncData({ store }) {
		// return store.dispatch('test');
	},
	computed: {

	},
});
</script>
