<template lang="slm">
div INDEX
</template>

<script lang="coffee">
import Vue from 'vue'

export default Vue.extend(
	name: 'Index'
	async_data: ({ store }) ->
		# store.dispatch('test')
)
</script>
